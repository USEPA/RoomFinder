import { IEnvironment } from '../app/shared/models/environment.model';

export const environment: IEnvironment = {
  name: 'dev',
  production: false,
  snackbarDuration: 500,
  baseApiUrl: 'https://localhost:44384/'
};
