export class DateFormatConstants {
    static readonly dateFormat = 'MM/dd/yyyy';
    static readonly dateTimeFormat = `${DateFormatConstants.dateFormat} HH:mm`;

    static readonly militaryDateFormat = 'dd MMM yyyy';
    static readonly militaryDateTimeFormat = `${DateFormatConstants.militaryDateFormat} HH:mm`;
}
